# 需求实施计划

> 基于 `.monkeycode-tmp-files/695f1473-敖胤AI-3D改版设计文档-1.md` (v2.0)
> 当前状态：Phase 1 已完成，Phase 2 R3F 跳过（改用纯 CSS/SVG），Phase 3-4 待实施

- [x] 1. 完善首页交互融合（对应 Phase 3、需求 3）
  - [x] 1.1 为首页 Hero 区域添加滚动视差效果
    - 在 `FullPageScroller` 的 `onScroll` 回调中计算滚动进度（0-1）。
    - 将进度传递给 `Hero` 组件，驱动标题和副标题的 `translateY` 和 `opacity` 变化。
    - 使用 `transform` 和 `opacity` CSS 属性，确保 GPU 加速。
    - 对应设计文档 Phase 3"首页首屏与 3D 场景建立视差关系"。
  - [x] 1.2 为分类卡片和文章卡片添加轻量空间悬停反馈
    - 在 `CategoryCard` 中使用 `whileHover` 增加 `translateZ` 或 `scale` 效果。
    - 在 `PostCard` 中使用 `whileHover` 增加轻微抬升和阴影变化。
    - 确保悬停反馈使用 `--dur-hover: 220ms` 和 `--ease-ink` 缓动。
    - 对应设计文档 Phase 3"栏目和文章卡片增加轻量空间反馈"。
  - [x] 1.3 优化首页四屏滚动节奏和视觉过渡
    - 为每个 `FullPageSection` 添加入场动画（`fadeInUp` 或 `scale`）。
    - 使用 Framer Motion 的 `whileInView` 触发动画，确保只在进入视口时播放。
    - 对应设计文档"低干扰动效"原则和"渐进感"设计目标。
  - [x] 1.4 编写首页交互测试
    - 验证滚动视差效果是否正确应用。
    - 验证卡片悬停反馈是否触发。
    - 验证入场动画是否在进入视口时播放。

- [x] 2. 实现探索页星盘布局（对应 Phase 3、需求 3）
  - [x] 2.1 创建探索页面 `app/explore/page.tsx`
    - 创建新的探索页面路由，展示主题栏目星盘布局。
    - 对应设计文档"探索页：主题栏目围绕中心渐卦形成二维星盘布局"。
  - [x] 2.2 实现桌面端星盘布局组件
    - 创建 `components/StarDiskLayout.tsx` 组件。
    - 将 8 个分类围绕中心渐卦符号排列成圆形或椭圆形。
    - 每个分类使用 `CategoryCard` 组件，保持链接卡片语义。
    - 对应设计文档"桌面端：主题栏目围绕中心渐卦形成二维星盘布局"。
  - [x] 2.3 实现移动端纵向列表布局
    - 在 `StarDiskLayout` 中检测视口宽度，小于 640px 时切换为纵向列表。
    - 保留图标、摘要和文章数量信息。
    - 对应设计文档"移动端：纵向列表，保留图标、摘要和文章数量"。
  - [x] 2.4 编写探索页布局测试
    - 验证桌面端星盘布局是否正确渲染 8 个分类。
    - 验证移动端是否切换为纵向列表。
    - 验证每个分类卡片是否可点击跳转。

- [x] 3. 优化文章详情页阅读体验（对应 Phase 3、需求 1.3）
  - [x] 3.1 确保文章详情页使用纯纸张阅读背景
    - 检查 `app/blog/[id]/page.tsx` 是否正确应用了纯纸张背景。
    - 移除文章详情页中可能存在的渐卦背景元素。
    - 对应设计文档"文章详情页：背景：纯纸张阅读"和需求 1.3"WHEN 访问者进入文章详情页, 系统 SHALL 呈现纯纸张阅读背景"。
  - [x] 3.2 添加文章目录和阅读进度组件
    - 创建 `components/TableOfContents.tsx` 组件，从 Markdown 标题生成目录。
    - 创建 `components/ReadingProgress.tsx` 组件，显示阅读进度条。
    - 对应设计文档"交互：目录、阅读进度和返回入口使用轻量 2D 组件"。
  - [x] 3.3 添加返回入口和导航优化
    - 在文章详情页顶部添加返回按钮或面包屑导航。
    - 确保移动端和桌面端都有清晰的返回路径。
    - 对应设计文档"返回入口使用轻量 2D 组件"。
  - [x] 3.4 编写文章详情页组件测试
    - 验证目录组件是否正确生成标题链接。
    - 验证阅读进度条是否随滚动更新。
    - 验证返回按钮是否可点击。

- [x] 4. 优化其他页面布局（对应 Phase 3、需求 1.2）
  - [x] 4.1 为展示页面共享 3D 背景
    - 检查关于、社区、路线图等页面是否应用了渐卦背景。
    - 确保这些页面的背景与首页一致。
    - 对应设计文档"关于、社区、路线图等展示页可共享 3D 背景"。
  - [x] 4.2 优化搜索、标签、归档和赛事统计页面布局
    - 确保这些页面使用稳定的内容布局，不被 3D 背景干扰。
    - 检查文本可读性和按钮可点击性。
    - 对应设计文档"搜索、标签、归档和赛事统计优先使用稳定的内容布局"。
  - [x] 4.3 编写页面布局一致性测试
    - 验证展示页面是否应用了渐卦背景。
    - 验证内容页面是否使用了稳定布局。

- [x] 5. 检查点 - 确保所有测试通过
  - 运行 `npx tsc --noEmit` 确认类型检查通过。
  - 运行 `npm run build` 确认静态导出完成。
  - 运行 `npm run test` 确认所有单元测试通过。
  - 对应设计文档验收清单第 7、8 项。

- [x] 6. 质量收敛与性能优化（对应 Phase 4）
  - [x] 6.1 执行 Playwright 截图回归测试
    - 启动开发服务器，使用 Playwright 截图首页、探索页、文章详情页。
    - 对比桌面端和移动端的截图，确保布局正确。
    - 对应设计文档 Phase 4"Playwright 截图回归"。
  - [x] 6.2 执行 Lighthouse 性能检查
    - 使用 Lighthouse 检查桌面端和移动端性能分数。
    - 确保 LCP、FID、CLS 等指标符合设计预算。
    - 对应设计文档 Phase 4"Lighthouse 桌面和移动性能检查"。
  - [x] 6.3 检查水合错误和运行时错误
    - 打开浏览器控制台，检查是否有水合错误或运行时错误。
    - 修复发现的任何错误。
    - 对应设计文档 Phase 4"检查水合错误、运行时错误和构建警告"。
  - [x] 6.4 清理未使用的 3D 实验组件
    - 检查 `components/` 目录，移除未使用的 3D 实验组件。
    - 确保 `AdaptiveScene.tsx` 和 `HexagramFallback.tsx` 被正确使用或移除。
    - 对应设计文档 Phase 4"清理未使用 3D 实验组件"。
  - [x] 6.5 编写质量验收测试
    - 编写 Playwright 测试脚本，自动化截图回归。
    - 编写 Lighthouse CI 配置，自动化性能检查。

- [x] 7. 最终验收（对应设计文档验收清单）
  - [x] 7.1 验证品牌一致性
    - 确认首页标题、Header、Footer 均显示"鸿渐Space"。
    - 确认渐卦六爻视觉正确显示。
    - 对应验收清单第 1 项。
  - [x] 7.2 验证导航和交互
    - 确认首页无圆点屏导航，键盘切屏继续可用。
    - 确认文章详情页保持纯阅读背景。
    - 对应验收清单第 2、4 项。
  - [x] 7.3 验证响应式布局
    - 确认桌面和移动端文本不溢出，按钮不遮挡内容。
    - 对应验收清单第 5 项。
  - [x] 7.4 验证技术指标
    - 确认 `npx tsc --noEmit` 通过。
    - 确认 `npm run build` 完成静态页面生成。
    - 确认无 `pageerror` 和 hydration 错误。
    - 对应验收清单第 6、7、8 项。
