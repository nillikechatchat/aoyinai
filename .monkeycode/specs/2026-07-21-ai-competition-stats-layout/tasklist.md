# 需求实施计划

- [x] 1. 建立赛事独立数据模块与追加式变更模型
  - [x] 1.1 在 `lib/competitions.ts` 定义赛事快照、更新记录、关键日期和状态类型，并迁移 `app/stats/page.tsx` 中的现有赛事数据
    - 为每场赛事分配稳定唯一的 `id`，保存核验日期、主办方来源链接和完整关键日期。
    - 为已有赛事的后续信息创建追加式 `changes` 记录，保留初始快照。
    - 对应需求 3.1、3.2、5.1；对应设计“赛事数据模块”和正确性属性 1、2、5。
  - [x] 1.2 实现赛事快照解析、活跃与归档派生、奖金统计和时间轴派生函数
    - 按日期顺序应用更新记录，返回展示用最新字段与最后更新时间。
    - 将缺少来源或有效截止日期的赛事标记为待核验，并从活跃统计、奖金图和时间轴排除。
    - 对应需求 2.1-2.4、3.3、3.5、4.3、5.3-5.4；对应设计“追加式更新规则”和正确性属性 3、4、6、7。
  - [x] 1.3 配置数据模块测试工具与赛事记录夹具
    - 增加适配 Next.js TypeScript 项目的测试脚本、测试环境和包含活跃、归档、待核验及多次修订场景的夹具。
    - 对应设计“Test Strategy”第 1-3 项。
  - [x] 1.4 编写赛事唯一标识和更新日期的属性测试
    - 验证生成的赛事集合保持唯一 `id`，每条更新记录日期满足初始核验日期边界。
    - 对应设计正确性属性 1、2。
  - [x] 1.5 编写追加式快照解析的属性测试
    - 验证任意排序的有效更新记录经日期排序后生成最后有效字段，且初始快照和更新历史保持不变。
    - 对应设计正确性属性 3、5。
  - [x] 1.6 编写活跃、归档和奖金统计的属性测试
    - 验证活跃集合、归档集合及奖金总额只接收满足核验、状态和日期规则的赛事。
    - 对应设计正确性属性 4、7；对应需求 2.1、3.5、5.3-5.4。
  - [x] 1.7 编写时间轴派生的属性测试
    - 验证时间轴保持日期升序，且每个节点关联活跃赛事记录。
    - 对应设计正确性属性 6；对应需求 1.4、2.4。

- [x] 2. 重构赛事统计页为主栏与侧边时间轴布局
  - [x] 2.1 修改 `app/stats/page.tsx` 使用赛事数据模块，展示数据核验日期、来源链接和最新有效赛事字段
    - 将概览、奖金比较、赛道分布和赛事详情表绑定到活跃赛事派生结果。
    - 在每条赛事详情中提供 `details/summary` 变更历史，按时间倒序展示摘要、字段变化、日期和来源。
    - 对应需求 1.5、2.2-2.3、3.3-3.4、5.2；对应设计“Stats 页面”。
  - [x] 2.2 实现桌面双栏和移动单栏的时间轴布局
    - 在 `lg` 断点建立 8 列主栏与 4 列侧栏，侧栏时间轴使用 `sticky top-24` 和受限滚动高度。
    - 在小于 `lg` 断点将时间轴渲染于赛事详情表前，并让时间轴节点链接至对应赛事行。
    - 对应需求 1.1-1.4；对应设计“Stats 页面”和“时间轴”。
  - [x] 2.3 增加折叠式归档赛事区域并复用赛事详情展示
    - 默认折叠归档区，展开后显示名称、赛道、奖金、截止日期、最终状态、最后更新时间和完整变更记录。
    - 对应需求 3.5、4.1-4.3；对应设计“Stats 页面”和“追加式更新规则”。
  - [x] 2.4 编写赛事统计页渲染测试
    - 验证活跃统计、来源链接、折叠归档区、可展开变更历史和移动端时间轴顺序。
    - 对应设计“Test Strategy”第 4 项；对应需求 1.3、3.4、4.1、5.2。

- [x] 3. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 运行赛事数据模块测试、页面渲染测试和 `npm run build`，确认静态导出、类型检查及 `/stats` 页面生成通过。
  - 对应设计“Test Strategy”第 5 项。
