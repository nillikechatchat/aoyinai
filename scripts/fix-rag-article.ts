/** rag-practice-guide 单篇定稿扩写（LLM 屡改 h2 标题，改为人工撰写），并去重插图 */
import { PrismaClient } from "@prisma/client";

const db = new PrismaClient();

const content = `## 一、问题的提出

客户知识库问答系统，基线版本命中率仅 61%。症状：检索回来的段落「相关但不用」，模型答非所问——引用了正确的文档，却说错了话。

更麻烦的是，这类错误极难复现：同一个问题换个问法，结果时对时错。我们先做了一件事：把线上三个月的真实提问拉出来，人工标注了 200 条「检索失败」样本，归为三类——问法口语化（42%）、答案跨多文档（31%）、专有名词未收录（27%）。后面的所有迭代，都围绕这三类展开。

## 二、十二次迭代中的关键五步

### 1. 切分策略重于模型选择

固定 512 token 切分是懒办法。按标题层级做结构化切分，保留章节路径作为元数据，命中率立涨 9 个百分点。

经验值：中文技术文档按 300~400 字切分、块间重叠 15% 左右最稳。表格务必整表保留，不要切——我们曾把一张 API 参数表切成两半，导致模型引用时永远缺列。

### 2. 混合检索是性价比之王

向量检索 + BM25 并行召回，RRF 融合。中文场景下 BM25 常被低估：产品型号、错误码这类「字面即答案」的查询，关键词检索命中率远高于向量。

我们的灰度数据：纯向量 61%，加 BM25 后 71%，成本几乎为零。

### 3. 重排模型的边际收益

接入 bge-reranker 后 Top-3 命中率提升 14%。代价是延迟增加约 120ms，值得。

但注意重排器的「口味」：它偏好长而完整的段落，与向量检索偏好短而精的块相反。两段式召回（先粗后精）正好把这对矛盾变成了互补。

### 4. 查询改写

用户的问题往往不适合直接检索。用小模型做一次 query rewrite，把口语转成检索友好的表达。

三类高频改写：省略主语的补全（「那部署呢？」→「RAG 服务如何部署？」）、多问合一的拆分（把复合问题拆成两个子查询分别检索）、术语归一（「向量库」→「向量数据库」）。仅此一项，跨文档类问题的命中率提升 11%。

### 5. 评测集先行

这可能是最反直觉的一条：先建评测集，再做优化。我们人工精选 120 条「问题-标准出处」对，每次迭代跑一遍，命中率变化一目了然。没有评测集的调优，全凭体感，十二次迭代根本无从谈起。

## 三、结语

92% 之后是什么？是「检不出来」变成了「答不好」：检索正确但生成时模型自由发挥。下一步我们把精力转向引用约束——强制模型只依据检索到的段落作答，并在答案后附出处编号。

回头看，这十二次迭代里没有一次魔法：结构化切分、混合检索、重排、改写、评测，全是朴素工程。RAG 的门槛不在算法前沿，而在把这些「已知有效」的动作踏实地做完。

> 千里之行，始于足下。检索之事，始于切块。

---

*欢迎在评论区交流你的 RAG 调优经验。*
`;

async function main() {
  const art = await db.article.findUnique({ where: { slug: "rag-practice-guide" } });
  if (!art) throw new Error("not found");
  // 以定稿文为准（已含全部小节）；插图固定插在第一小节末段之后
  const final = content.replace(
    "都围绕这三类展开。",
    "都围绕这三类展开。\n\n![墨池试索，一网千钩](/images/illu-tutorials.png)"
  );
  const chars = final.replace(/\s/g, "").length;
  const readMinutes = Math.max(1, Math.round(chars / 250));
  await db.article.update({
    where: { slug: "rag-practice-guide" },
    data: { content: final, readMinutes },
  });
  console.log(`rag-practice-guide 定稿: ${chars} 字 / ${readMinutes} 分钟 / 插图 ${(final.match(/!\[/g) || []).length} 处`);
  await db.$disconnect();
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
